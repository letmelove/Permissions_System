#数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/shirodemo?characterEncoding=utf8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
  mvc:
    view:
      prefix: /pages/
      suffix: .jsp

#mybatis
mybatis:
  mapper-locations: mapper/*.xml
  type-aliases-package: com.shiro.shiroDemo.model
